import { Layout, SEO, Image, Card } from "../components"

import { useSiteMetadata } from "../hooks"

import { Link } from "gatsby"

export default ({ location, children }) => {
  const { description } = useSiteMetadata()
  return (
    <Layout location={location}>
      <article className="page">
        <SEO title="Home" />
        <div className="content-aside-grid">
          <div className="content padding">{children}</div>
          <aside className="aside padding">
            <a className="nav-link" href="#typography">
              <h3>Typography</h3>
            </a>
            <a className="nav-link" href="#colors">
              <h3>Colors</h3>
            </a>
            <a className="nav-link" href="#spacing">
              <h3>Spacing</h3>
            </a>
            <a className="nav-link" href="#components">
              <h3>Components</h3>
            </a>
            <a className="nav-link" href="#layout">
              <h3>Layout</h3>
            </a>
          </aside>
        </div>
      </article>
    </Layout>
  )
}

```jsx
<div className="card">
  <h1 className="card-heading text--xxxl">
    Build Responsive UIs with One Stylesheet and CSS Variables
  </h1>
  <Image />
  <p className="card-text">
    Featuring responsive typography and spacing, adjustable vertical rhythm,
    light / dark themes by default. Customize with CSS variables.
  </p>
</div>
```

---

<h2 className="title" id="typography">
  Typography
</h2>

```jsx
<>
  <h1 className="title text--xxxl">Large Title Heading One</h1>
  <h1 className="title">Title Heading One</h1>
  <h1>Heading One</h1>
  <h2>Heading Two</h2>
  <h3>Heading Three</h3>
  <h4>Heading Four</h4>
  <small>Small</small>
  <P />
</>
```

---

<h2 className="title" id="colors">
  Colors
</h2>

```jsx
<div className="content-grid">
  <div
    style={{
      background: `var(--text-color)`,
      color: `var(--accent-1)`,
      padding: `var(--space-lg)`,
      borderRadius: `var(--radius)`,
      boxShadow: `var(--elevation-1)`,
    }}
  >
    --text-color
  </div>
  <div
    style={{
      background: `var(--bg-1)`,
      padding: `var(--space-lg)`,
      borderRadius: `var(--radius)`,
      boxShadow: `var(--elevation-1)`,
    }}
  >
    --bg-1
  </div>
  <div
    style={{
      background: `var(--bg-2)`,
      padding: `var(--space-lg)`,
      borderRadius: `var(--radius)`,
      boxShadow: `var(--elevation-1)`,
    }}
  >
    --bg-2
  </div>
  <div
    style={{
      background: `var(--grey)`,
      padding: `var(--space-lg)`,
      borderRadius: `var(--radius)`,
      boxShadow: `var(--elevation-1)`,
    }}
  >
    --grey
  </div>
  <div
    style={{
      background: `var(--accent-1)`,
      padding: `var(--space-lg)`,
      borderRadius: `var(--radius)`,
      boxShadow: `var(--elevation-1)`,
      color: `var(--accent-2)`,
    }}
  >
    --accent-1
  </div>
  <div
    style={{
      background: `var(--accent-2)`,
      padding: `var(--space-lg)`,
      borderRadius: `var(--radius)`,
      boxShadow: `var(--elevation-1)`,
      color: `var(--accent-1)`,
    }}
  >
    --accent-2
  </div>
  <div
    style={{
      background: `var(--code-bg)`,
      padding: `var(--space-lg)`,
      borderRadius: `var(--radius)`,
      boxShadow: `var(--elevation-1)`,
      color: `var(--accent-1)`,
    }}
  >
    --code-bg
  </div>
</div>
```

---

<h2 className="title" id="spacing">
  Spacing
</h2>

```jsx
<div>
  <div
    className="card"
    style={{
      marginBottom: `var(--space-sm)`,
      padding: `var(--space-sm)`,
    }}
  >
    --space-sm
  </div>

  <div
    className="card"
    style={{
      marginBottom: `var(--space-md)`,
      padding: `var(--space-md) var(--space-sm)`,
    }}
  >
    --space-md
  </div>

  <div
    className="card"
    style={{
      marginBottom: `var(--space-lg)`,
      padding: `var(--space-lg) var(--space-sm)`,
    }}
  >
    --space-lg
  </div>

  <div
    className="card"
    style={{
      marginBottom: `var(--space-xl)`,
      padding: `var(--space-xl) var(--space-sm)`,
    }}
  >
    --space-xl
  </div>
</div>
```

---

<h2 className="title" id="components">
  Components
</h2>
<br />
<nav className="nav">
  <a className="nav-link" href="#button">
    <h3>Button</h3>
  </a>
  <a className="nav-link" href="#card">
    <h3>Card</h3>
  </a>
  <a className="nav-link" href="#fieldset">
    <h3>Fieldset</h3>
  </a>
  <a className="nav-link" href="#figure">
    <h3>Figure</h3>
  </a>
  <a className="nav-link" href="#navlink">
    <h3>Navlink</h3>
  </a>
  <a className="nav-link" href="#navlinks">
    <h3>Navlinks</h3>
  </a>
  <a className="nav-link" href="#textarea">
    <h3>Textarea</h3>
  </a>
</nav>

---

<h3 id="button">{`<Button/>`}</h3>

```jsx
<div>
  <button className="btn">Button</button>
  <button className="btn btn-primary">Primary Button</button>
  <button className="btn" disabled={true}>
    Button Bisabled
  </button>
  <button className="btn btn-primary" disabled={true}>
    Primary Button Disabled
  </button>
</div>
```

---

<h3 id="card">{`<Card />`}</h3>

```jsx
<div className="card">
  <h2 className="card-heading">Heading</h2>
  <Image />
  <p className="card-text">Text</p>
</div>
```

---

<h3 id="fieldset">{`<Fieldset />`}</h3>

```jsx
<fieldset className="fieldset">
  <form className="form" action="">
    <input className="input" type="email" placeholder="Input" />
    <button className="btn btn-primary" type="submit">
      Primary Button
    </button>
  </form>
</fieldset>
```

---

<h3 id="figure">{`<Figure />`}</h3>

```jsx
<figure className="figure">
  <Image />
  <figcaption className="figcaption">Figcaption</figcaption>
</figure>
```

---

<h3 id="navlink">{`<Navlink />`}</h3>

```js
function Navlink({
  text = "Navlink",
  to = "/components",
  // Mocked reach router location object
  location = { pathname: "/" },
}) {
  return (
    <Link
      className="nav-link"
      data-is-active={location.pathname === to}
      to={to}
    >
      {text}
    </Link>
  )
}
```

---

<h3 id="navlinks">{`<Navlinks />`}</h3>

TODO

---

<h3 id="textarea">{`<Textarea />`}</h3>

```jsx
<form className="form">
  <textarea className="textarea" />
  <button className="btn btn-primary">Submit</button>
</form>
```

---

<h2 className="title" id="layout">
  Layout
</h2>
<br />
<nav className="nav">
  <a className="nav-link" href="#app">
    <h3>App</h3>
  </a>
  <a className="nav-link" href="#content-grid">
    <h3>Content-Grid</h3>
  </a>
  <a className="nav-link" href="#content-aside-grid">
    <h3>Content-Aside-Grid</h3>
  </a>
  <a className="nav-link" href="#image-grid">
    <h3>Image-Grid</h3>
  </a>
</nav>

---

<h3 id="app">{`<App />`}</h3>

```jsx
<div>
  <header
    className="header padding"
    style={{
      background: `var(--accent-2)`,
    }}
  >
    <h4 className="site-title">Site Title </h4>
  </header>
  <main
    className="main"
    style={{
      background: `var(--accent-1)`,
    }}
  >
    <h4 className="padding">Main</h4>
  </main>
  <footer
    className="footer padding"
    style={{
      background: `var(--accent-2)`,
    }}
  >
    <h4 className="site-title">Footer</h4>
  </footer>
</div>
```

---

<h3 id="content-grid">{`<Content-Grid />`}</h3>

```js
<div className="content-grid">
  <Card />
  <Card />
  <Card />
  <Card />
  <Card />
  <Card />
  <Card />
  <Card />
</div>
```

---

<h3 id="content-aside-grid">{`<Content-Aside-Grid />`}</h3>

```jsx
<div className="content-aside-grid">
  <div
    className="content"
    style={{
      background: `var(--accent-1)`,
      color: `var(--accent-2)`,
      boxShadow: `var(--elevation-2)`,
      borderRadius: `var(--radius)`,
    }}
  >
    Content
  </div>
  <aside
    className="aside"
    style={{
      background: `var(--accent-2)`,
      boxShadow: `var(--elevation-1)`,
      borderRadius: `var(--radius)`,
    }}
  >
    Aside
  </aside>
</div>
```

---

<h3 id="image-grid">{`<ImageGrid />`}</h3>

```jsx
<div className="image-grid only-fullscreen-padding">
  <Image />
  <Image />
  <Image />
  <Image />
  <Image />
  <Image />
  <Image />
  <Image />
  <Image />
</div>
```

---
